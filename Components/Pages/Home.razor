@page "/"

<div class="center-container">
    <h1 class="welcome-title">
        <img src="img/oip.jfif" class="welcome-icon" alt="Optometría" />
        Bienvenido al Centro de Optometría
    </h1>
</div>

<p class="description">Selecciona la opción que desees para gestionar pacientes, doctores y más.</p>

<div class="menu">
    <div class="menu-item">
        <a href="/pacientes" class="menu-link">
            <img src="img/Paciente.png" alt="Pacientes" class="menu-icon" />
            <span class="menu-text">Pacientes</span>
        </a>
    </div>
    <div class="menu-item">
        <a href="/doctores" class="menu-link">
            <img src="img/Doctor.png" alt="Doctores" class="menu-icon" />
            <span class="menu-text">Doctores</span>
        </a>
    </div>
    <div class="menu-item">
        <a href="/add-examen" class="menu-link">
            <img src="img/examen.png" alt="Examen" class="menu-icon" />
            <span class="menu-text">Examen</span>
        </a>
    </div>
  
</div>



@code {
    // Puedes agregar código adicional aquí si es necesario
}

<style>
    h1 {
        text-align: center;
        color: #007bff;
        font-family: 'Arial', sans-serif;
        margin-bottom: 20px;
    }

    .description {
        text-align: center;
        color: #555;
        font-size: 1.2em;
        margin-bottom: 30px;
    }

    .menu {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-bottom: 30px;
    }

    .menu-item {
        text-align: center;
        border: 2px solid #007bff;
        border-radius: 10px;
        padding: 20px;
        background-color: #f8f9fa;
        transition: background-color 0.3s, transform 0.3s;
        width: 150px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

        .menu-item:hover {
            background-color: #e9ecef;
            transform: scale(1.05);
        }

    .menu-link {
        text-decoration: none;
        color: #007bff;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .menu-icon {
        width: 60px;
        height: 60px;
        margin-bottom: 10px;
    }

    .menu-text {
        font-size: 1.1em;
    }

    .hero-image {
        display: block;
        margin: 0 auto;
        width: 100%;
        max-width: 600px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .center-container {
        display: flex;
        justify-content: center; /* Centra el contenedor horizontalmente */
        align-items: center; /* Centra el contenedor verticalmente */
        height: 30vh; /* Ocupa el 100% de la altura de la ventana */
        text-align: center; /* Alinea el texto al centro */
    }
    .welcome-title {
        display: flex;
        align-items: center; /* Alinea verticalmente la imagen y el texto */
        font-size: 24px; /* Tamaño de fuente del título */
    }

    .welcome-icon {
        width: 120px; /* Ancho de la imagen */
        height: auto; /* Mantiene la proporción de la imagen */
        margin-right: 10px; /* Espacio entre la imagen y el texto */
    }
</style>